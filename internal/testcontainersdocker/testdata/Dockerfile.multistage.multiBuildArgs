ARG BASE_IMAGE=docker.io/scratch
ARG NGINX_IMAGE=docker.io/nginx:latest
ARG REGISTRY_HOST=localhost
ARG REGISTRY_PORT=5000
FROM ${NGINX_IMAGE} as builderA
FROM ${REGISTRY_HOST}:${REGISTRY_PORT}/${NGINX_IMAGE} as builderB
FROM ${BASE_IMAGE}
