{{ $lower := .Name | ToLower }}
package {{ $lower }}

import (
	"context"
	"testing"
)

func Test{{ .Name }}(t *testing.T) {
	ctx := context.Background()

	container, err := setup{{ .Name }}(ctx)
	if err != nil {
		t.Fatal(err)
	}

	// Clean up the container after the test is complete
	defer container.Terminate(ctx)

	// perform assertions
}
